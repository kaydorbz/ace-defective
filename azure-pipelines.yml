trigger:
- main

pool:
  name: 'Default'  # This targets your local computer

steps:
- script: |
    echo "--- Setting up Python Environment ---"
    python -m pip install --upgrade pip
    pip install pytest
    pip install -r requirements.txt
    
    echo "--- Running Tests ---"
    pytest
  displayName: 'Build and Test'